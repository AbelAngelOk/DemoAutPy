pipeline {
    agent any

    stages {
        stage('enviroment build') {
            steps {
                script {
                    sh "EJECUTANDO..."
		    sh "python --version"
		    sh cd "$(mktemp -d)"
                }
            }
        }
        stage('docker push') {
            steps {
                script {
                    sh ""
                }
            }
        }
    }
}


